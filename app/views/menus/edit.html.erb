<h1>Editing Menú <b> <%= @menu.title%> </b></h1>

<div class="form-box">
  <%= form_for [@menu] do |f| %>
    <%= render :partial => 'form', :locals => {:f => f} %>
    
    <div class="actions">
      <%= f.submit :update %>
      or
      <%= link_to 'cancel', menus_path %>
    </div>
  <% end %>
</div>